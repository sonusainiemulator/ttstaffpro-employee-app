# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Local-only mock Firebase iOS config (no real iOS app registered yet).
# Replace with the real file from Firebase Console before shipping.
/ios/Runner/GoogleService-Info.plist
/firebase.json

# Build artifacts & local machine config (never commit)
/*.apk
/*.aab
/android/build/

# SSH connection helper (local, references hosts/credentials)
/ssh-config.ttstaffpro

# Standalone sub-apps live in their own GitHub repos
/apps/
